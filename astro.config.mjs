import { defineConfig } from 'astro/config';

// Cấu hình tối giản tuyệt đối
export default defineConfig({
  site: "https://UyenThyyy.github.io", // Your site URL
  base: "/", // Subpath (for GitHub Pages deployment)
  // ...
});
